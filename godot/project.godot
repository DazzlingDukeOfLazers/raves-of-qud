; Engine configuration file.
; It's best edited using the editor UI and not directly,
; since the parameters that go here are not all obvious.
;
; Format:
;   [section] ; section goes between []
;   param=value ; assign values to parameters

config_version=5

[animation]

compatibility/default_parent_skeleton_in_mesh_instance_3d=true

[application]

config/name="Raves of Qud"
run/main_scene="res://Main.tscn"
config/features=PackedStringArray("4.7", "Forward Plus")

[display]

; A logical 1600x900 window rendered at NATIVE pixel density (allow_hidpi) is the
; real fix for the "aliased text": on a Retina Mac, hidpi=false renders at logical
; resolution and lets macOS upscale -> blur. hidpi=true draws at physical pixels ->
; crisp, without a 3840-wide window the OS then clamps and appears to "jump".
; mode=2 starts MAXIMISED, so it fills whatever screen is attached (the real want
; behind setting 3840) without a fixed size the display can't satisfy. 1600x900 is
; just the restore size when un-maximised. Resizable, so drag it however you like.
window/size/viewport_width=1600
window/size/viewport_height=900
window/size/resizable=true
window/size/mode=2
window/dpi/allow_hidpi=true

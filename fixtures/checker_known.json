{
 "Chiliad Creature Newly Sentient Beings": "NREs on bare staging (procedural-history creature; needs world context) â€” reports/2026-08-03-checker-chiliad-npe.md",
 "Marble Dais": "multi-cell sprite: the stage-cell crop catches a fraction; needs a multi-cell staging rule",
 "E Marble Dais": "multi-cell sprite (dais family)",
 "E1 Marble Dais": "multi-cell sprite (dais family)",
 "E2 Marble Dais": "multi-cell sprite (dais family)",
 "E3 Marble Dais": "multi-cell sprite (dais family)",
 "W Marble Dais": "multi-cell sprite (dais family)",
 "W1 Marble Dais": "multi-cell sprite (dais family)",
 "W2 Marble Dais": "multi-cell sprite (dais family)",
 "W3 Marble Dais": "multi-cell sprite (dais family)",
 "N Marble Dais": "multi-cell sprite (dais family)",
 "S Marble Dais": "multi-cell sprite (dais family)",
 "S1 Marble Dais": "multi-cell sprite (dais family)",
 "S2 Marble Dais": "multi-cell sprite (dais family)",
 "TauSoft": "oversized/off-anchor sprite: 100% hot crop â€” needs the multi-cell staging rule",
 "CherubimSpawn1B": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn2A Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn2B": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn3A Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn5A": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn1B Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn1A": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn1A Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn2A": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn2B Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn3A": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn3B": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn3B Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn4A": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn4A Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn4B": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn4B Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn5A Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn5B": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)",
 "CherubimSpawn5B Special": "CherubimSpawner: replaces itself with the real cherub between the two captures — staging-timing, unverifiable by same-turn pair; the spawned cherub is never pacified (one killed the player mid-sweep)"
}